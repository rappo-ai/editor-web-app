/*
 * PlayerPage Messages
 *
 * This contains all the text for the PlayerPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PlayerPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PlayerPage container!',
  },
});
