/*
 * BotsList Messages
 *
 * This contains all the text for the BotsList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BotsList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BotsList component!',
  },
});
